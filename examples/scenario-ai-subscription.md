# Scenario: AI Subscription

User input:

```text
帮我优化这个问题：Claude 和 GPT 我应该买哪个会员？
```

Expected scenario:

- ai_subscription_choice

Example upgraded prompt:

> 我在考虑订阅 Claude Pro 或 ChatGPT Plus。我的主要用途是 `[用途]`，每月预算是 `[预算]`，我对隐私/联网/代码/长文档/中文输出的要求是 `[要求]`。请基于我的使用场景做选择，而不是泛泛比较。输出：推荐结论、适用条件、放弃另一个选项的理由、反例场景、7 天验证计划，以及如果我只能先买一个应该怎么试错。
